[project]
name = "frontend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hrshvrdhn26", email = "hrshvrdhn26@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
frontend = "frontend:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
